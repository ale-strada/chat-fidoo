import ResetPasswordForm from "@/components/resetpassword";

export default function ResetPassword() {
  
    return (
      <main className="flex min-h-screen flex-col items-center justify-between px-10 py-24">
          <ResetPasswordForm />
      </main>
    );
  }
